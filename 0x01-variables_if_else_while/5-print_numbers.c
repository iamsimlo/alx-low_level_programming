#include <stdio.h>
/**
 * main - Entry point
 * Program print all single digits from 0-10
 *
 * Return: always 0(success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
}
