#include "main.h"
/**
 * mul - function to multiply 2 integers
 * @a: is the 1st integer parameter
 * @b: is the second interger parameter
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);

}
